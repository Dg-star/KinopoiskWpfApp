<Page x:Class="KinopoiskWpfApp.Views.FavoritesPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Избранное">

    <Grid Margin="20">
        <StackPanel>
            <TextBlock Text="Избранные фильмы" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>

            <!-- Фильтры -->
            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                <ComboBox Width="200"
                          ItemsSource="{Binding Genres}"
                          DisplayMemberPath="Name"
                          SelectedItem="{Binding SelectedGenre}" 
                          Margin="0,0,10,0"/>
                <ComboBox Width="200"
                          ItemsSource="{Binding Countries}"
                          DisplayMemberPath="Name"
                          SelectedItem="{Binding SelectedCountry}"/>
            </StackPanel>

            <!-- Список фильмов -->
            <ListBox ItemsSource="{Binding FilteredFavoriteFilms}" Height="400">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                            <TextBlock Text="{Binding NameRu}" FontWeight="Bold" Width="300"/>
                            <TextBlock Text="{Binding Year}" Margin="10,0,0,0" Width="50"/>
                            <Button Content="Удалить"
                                    Command="{Binding DataContext.RemoveFromFavoritesCommand, RelativeSource={RelativeSource AncestorType=Page}}"
                                    CommandParameter="{Binding}" Margin="10,0,0,0"/>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </StackPanel>
    </Grid>
</Page>
